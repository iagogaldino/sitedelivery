import { OrdenarPorPipe } from './ordenar-por.pipe';

describe('OrdenarPorPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenarPorPipe();
    expect(pipe).toBeTruthy();
  });
});
